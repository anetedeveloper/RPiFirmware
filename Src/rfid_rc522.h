#ifndef RFID_RC522_H
#define RFID_RC522_H
/*

#include <QProcess>
#include <QDebug>

class RFID_RC522: public QObject
{
    Q_OBJECT
public:
    RFID_RC522();
    void StartReading();
    void SetStartKey(int StartKey);
    void SetStopKey(int StopKey);
signals:
    void CardReaded(QString cardNumber);
private slots:
    void handleRead();
private:
      int StartKey = 2;
      int StopKey = 3;
      QString CardNumber;
      QProcess *proc;
};
*/
#endif // RFID_RC522_H
